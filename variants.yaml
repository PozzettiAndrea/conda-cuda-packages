cuda_compiler_version:
  - "12.6"
  - "12.8"
  - "12.9"
  - "13.0"

python:
  - "3.10"
  - "3.11"
  - "3.12"
  - "3.13"

cuda_compiler:
  - cuda

# Arch list per CUDA version (matched via zip_keys would be ideal,
# but rattler-build uses if/else in the recipe instead)
