# Static variant config for rattler-build
# The actual build matrix comes from packages/*.yml via generate_matrix.py
# rattler-build uses --variant flags from CI to select the specific combo

c_stdlib_version:
  - "2.28"

c_stdlib:
  - sysroot

cuda_compiler:
  - cuda
